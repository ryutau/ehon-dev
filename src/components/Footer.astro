---
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const link = (path: string) => `${base}${path}`;
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container site-footer__inner">
    <div>
      <p class="site-footer__brand">えほんの森</p>
      <p class="muted">絵本と人との出会いを、ゆっくり育む場所。</p>
    </div>
    <nav class="site-footer__nav" aria-label="フッター">
      <ul>
        <li><a href={link("/")}>トップ</a></li>
        <li><a href={link("/activities/")}>最近の活動</a></li>
        <li><a href={link("/about/")}>えほんの森について</a></li>
        <li><a href={link("/contact/")}>お問い合わせ</a></li>
      </ul>
    </nav>
  </div>
  <div class="container site-footer__copy">
    <small>&copy; {year} えほんの森</small>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: var(--space-9);
    background: var(--color-accent-soft);
    border-top: 1px solid var(--color-border);
    padding-block: var(--space-7);
  }
  .site-footer__inner {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: var(--space-5);
    align-items: start;
  }
  .site-footer__brand {
    font-weight: 700;
    margin-bottom: var(--space-2);
  }
  .site-footer__nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
  }
  .site-footer__nav a {
    color: var(--color-text);
    text-decoration: none;
    font-size: 0.95rem;
  }
  .site-footer__nav a:hover {
    color: var(--color-accent-strong);
  }
  .site-footer__copy {
    margin-top: var(--space-5);
    color: var(--color-muted);
  }
  @media (max-width: 640px) {
    .site-footer__inner {
      grid-template-columns: 1fr;
    }
  }
</style>
